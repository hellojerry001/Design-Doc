---
sidebar: false
---

# <接口名>

> <版本> ｜ 调用方 `<调用方文件>` ｜ `<权限要求 | 无需权限>`

## 功能描述

一句话说明这个接口**做了什么**、**用在什么场景**。

## 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `arg1` | string | 是 | |
| `arg2` | object | 否 | |

> 无参数时写「无」。

## 返回值

返回类型与字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `field1` | string | |
| `field2` | object[] | |

## 调用示例

```js
<一段能直接贴到项目里跑的示例>
```

## 实现位置

`<源文件路径>:<行号>` — `<函数 / 路由 / 命令声明>`

```<语言>
<对应的最小实现片段，或路由注册那一行>
```

## 注意事项

1. `tip`：边界条件、错误码、常见踩坑。

## 相关接口

- [`<相关接口 A>`](/api/<a>)
- [`<相关接口 B>`](/api/<b>)
