# <组件名> <中文名>

> 来源 `<源文件路径>` ｜ 适用：<使用场景 / 所在作用域> ｜ <依赖的变量前缀，如 `--dq-*`（需在预览作用域内）>

## 功能描述

一句话说明这个组件**做什么**、**用在什么场景**。不要复述参数表 —— 参数表在下面。

## 实时预览

<DemoBlock title="<组件名> · <变体说明>" source="<sources 的键，可省>">
  <LiveComponent asset="<LOADERS 的键>" create="<命名空间>.<工厂函数>" :args="[{ }]" />
</DemoBlock>

<!--
  写预览的三条要求：
  1) 必须调用**真实组件源码**，不要重写一个长得像的假组件。
  2) 变体多的时候用 <ComponentShowcase>（见 templates/gallery-page.md），
     而不是在这里堆十来个 DemoBlock。
  3) 组件加载不出来时，LiveComponent 会就地显示错误原因 —— 别把它删掉换成截图。
-->

## API

### `<命名空间>.<工厂函数>(opts)`

| 参数 | 类型 | 默认值 | 必填 | 说明 |
|------|------|--------|------|------|
| `xxx` | `'a' \| 'b'` | — | 是 | |
| `yyy` | string | `''` | 否 | |

<返回值：类型、字段，以及"真正的 DOM 在 .el 上"这类容易踩的点>

```js
<一段能直接跑的调用示例>
```

## 注意事项

1. `tip`：边界条件、常见踩坑、与相邻组件的差异。一条一行，别写散文。
2. `tip`：样式依赖哪些 token、在什么作用域内才生效。

## 相关组件

- [<相邻组件 A>](/components/<a>)
- [组件总览](/components/overview)
